import React from 'react';
import { View } from 'react-native';

import { Text } from '@/components/ui';

const Scan = () => {
  return (
    <View>
      <Text>Scan</Text>
    </View>
  );
};

export default Scan;
